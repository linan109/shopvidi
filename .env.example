# ShopVidi 环境配置

# N8N Webhook URL（必须配置）
VITE_N8N_WEBHOOK_URL=https://n8n.merakku.ai/webhook/analyse

# N8N API Key（可选，用于认证）
VITE_N8N_API_KEY=

# API 版本号（用于缓存版本控制）
# ⚠️ 当 N8N workflow 有重大变更时，更新此版本号可自动清除旧缓存
VITE_API_VERSION=1.0.0

# 说明：
# - 系统默认使用 N8N API，无需额外配置
# - 当 N8N 请求失败时，自动使用 IndexedDB 缓存降级
# - 缓存有效期：7 天
# - 缓存版本控制：只有版本匹配的缓存才会被使用

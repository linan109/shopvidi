# ShopVidi 环境配置

# 使用 Mock API（开发阶段设为 true，生产环境不设置或设为 false）
# 不设置此变量时，默认使用真实 N8N API
VITE_USE_MOCK=true

# N8N Webhook URL（生产环境必须配置）
VITE_N8N_WEBHOOK_URL=https://n8n.merakku.ai/webhook/analyse

# N8N API Key（可选，用于认证）
VITE_N8N_API_KEY=

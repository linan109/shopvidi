# ShopVidi 环境配置

# 使用 Mock API（开发阶段设为 true）
# 设为 false 后将使用真实 N8N API
VITE_USE_MOCK=true

# N8N Webhook URL（连接真实 N8N 时需要配置）
VITE_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/shop-analyzer

# N8N API Key（可选，用于认证）
VITE_N8N_API_KEY=
